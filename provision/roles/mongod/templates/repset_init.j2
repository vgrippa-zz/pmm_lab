{% for host in groups['replication_servers'] %}
rs.initiate({
    _id: "{{ host }}",
    version: 1,
    members: [
       { _id: 0, host : "{{ host }}:{{ mongod_port }}" }
       ]
})
sleep(3000)
{% endfor %}
